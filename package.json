{
  "name": "pulseboard",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.js",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0"
  },
  "devDependencies": {
    "vitest": "^4.1.10",
    "@vitest/coverage-v8": "^4.1.10"
  }
}
